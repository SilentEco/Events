@page
@model Inlämningsuppgift1.Pages.MyEventsModel

@{
    ViewData["Title"] = "MyEvents";
}

<h1>MyEvents</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Attendee.name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Attendee.email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Attendee.phone_number)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Attendee.events) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.place)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.adress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.adress)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.eventID">Home</a> |

            </td>
        </tr>
}
    </tbody>
</table>
